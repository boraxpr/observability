prometheus.exporter.self "default" {
}

prometheus.scrape "metamonitoring" {
  targets    = prometheus.exporter.self.default.targets
  forward_to = [prometheus.remote_write.default.receiver]
}

prometheus.remote_write "default" {
  endpoint {
    url = "http://prometheus:9090/api/v1/write"
  }
}

logging {
  level    = "info"
  format   = "json"
  write_to = [loki.write.default.receiver]
}

loki.write "default" {
  endpoint {
    url = "http://loki:3100/loki/api/v1/push"
  }
}

/*
otelcol.receiver.otlp "app1" {
    http {
        endpoint = "0.0.0.0:8002"
  }
  	output {
		metrics = [otelcol.processor.batch.app1.input]
		logs    = [otelcol.processor.batch.app1.input]
		traces  = [otelcol.processor.batch.app1.input]
	}
}

otelcol.processor.batch "app1" {
  	output {
		metrics = [otelcol.exporter.otlp.app1.input]
		logs    = [otelcol.exporter.otlp.app1.input]
		traces  = [otelcol.exporter.otlp.app1.input]
	}
}

otelcol.exporter.loki "app1" {
  forward_to = [loki.write.app1.receiver]
}

loki.write "app1" {
  endpoint {
    url = "http://loki:3100/loki/api/v1/push"
  }
}

otelcol.exporter.prometheus "app1" {
  forward_to = [prometheus.remote_write.app1.receiver]
}

prometheus.remote_write "app1" {
  endpoint {
    url = "http://prometheus:9090/api/v1/write"
  }
}

otelcol.exporter.otlp "app1" {
  client {
    endpoint = "http://jaeger:4318"
    tls {
      insecure = true
    }
  }
}
*/
